export enum MeasureUnit {
  KG = 'kg', // kilogramos
  G = 'g', // gramos
  MG = 'mg', // miligramos

  L = 'l', // litros
  ML = 'ml', // mililitros
  CM3 = 'cm3', // centímetros cúbicos

  UNIT = 'unit', // unidad (ej: 1 paquete, 1 item)
}
